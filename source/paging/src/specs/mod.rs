//! Specifications for unverified external surface, vendored from COCONUT-SVSM's
//! `verify_external` crate, reduced to what this crate uses.
#[cfg(verus_only)]
pub mod entry;
pub mod external;
pub mod nonnull;
